__pycache__/
*.py[cod]
*.egg-info/
.venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/
dist/
build/
*.duckdb
*.duckdb.wal
findings.sarif
.coverage
htmlcov/
.DS_Store

# Raw screen recordings live outside the repo (transcoded mp4s are committed
# selectively). The macOS default filename includes a U+202F NARROW NO-BREAK
# SPACE so we match by glob.
*.mov
Screen Recording*

# Local secrets. Never commit. Use .env.example as the template.
.env
.env.*
!.env.example
